#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)

# COMPONENT_EMBED_TXTFILES adds null byte at the end
# COMPONENT_EMBED_FILES does not add a null byte

COMPONENT_EMBED_FILES := index.html.gz
